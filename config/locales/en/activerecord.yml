en:
  activerecord:
    errors:
      models:
        accounting/user_withdrawal:
          attributes:
            user:
              account_no_money: "has no money in account (available account balance is %{available_account_balance})"
            amount:
              bigger_than_available_account_balance: "must be smaller than the available account balance (%{available_account_balance})"
